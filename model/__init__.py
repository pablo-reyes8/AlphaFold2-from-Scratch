"""Core AlphaFold2-style model modules exposed as a regular Python package.

This package keeps the architecture split into inspectable submodules so the
repo can be installed in editable mode without relying on path hacks.
"""
