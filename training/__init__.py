"""Training utilities for optimization, metrics, checkpointing, and runtime.

The package is intentionally lightweight so the repository can be installed and
its training entry points imported without implicit path manipulation.
"""
